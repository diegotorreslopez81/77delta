---
/**
 * La Δ de la marca como SVG inline. Sora no tiene glifo Δ y el navegador la
 * sustituiría por la fuente del sistema; así es idéntica en todas partes.
 * GENERADO por scripts/brand-svg.mjs: no editar a mano.
 */
interface Props {
  class?: string;
}
const { class: extra = '' } = Astro.props;
---

<svg
  viewBox="0 0 752 730"
  class:list={['inline-block h-[0.73em] w-auto align-baseline fill-current', extra]}
  aria-hidden="true"
  focusable="false"
>
  <path fill-rule="evenodd" transform="translate(0 730.0)" d="M376.1 -730.0L719.2 0.0L33.0 0.0ZM376.1 -421.6L512.6 -131.2L239.6 -131.2Z"></path>
</svg>
